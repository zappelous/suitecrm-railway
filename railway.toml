[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "/entrypoint.sh apache2-foreground"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
